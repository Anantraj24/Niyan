# NIYAM-X Antigravity Context Pack

This pack is designed so coding agents do not need to repeatedly scan the entire NIYAM-X repository.

Start with:

`00_START_HERE.md`

For a one-shot architecture context, use:

`MASTER_CONTEXT.md`

Recommended location inside the real project:

```text
docs/antigravity/
```

The source-of-truth rule is:

**docs define contracts; code implements contracts. If they disagree, stop and reconcile instead of silently diverging.**
